# 设计与学习记录

## 使用方法

本文件用于记录自己的理解、设计理由和验收证据；项目的安装与使用说明放在 [README](../README.md)。

每个 Issue 使用相同结构：学习范围与文件、个人总结、问题记录、验收与归档。保留原有的 Issue #1 设计记录，其余个人总结留待学习时填写。

“待填写”是提示，不代表功能已经实现或验收通过。测试结果和提交编号填写真实记录；当前代码进度见 README。

## 项目目标

实现一个正确、可理解、可验证的 C++17 线程池。各阶段先完成当前验收，再推进下一阶段。

## Issue #1：泛型线程安全任务队列

[查看 Issue](https://github.com/Zhihui-Cui/cpp-thread-pool/issues/1)

### 学习范围与文件

- 学习范围：mutex、lock_guard、模板、optional；实现安全存取与边界情况测试。
- 文件位置：`include/task_queue.hpp`、`tests/task_queue_test.cpp`

### 已有设计记录

- `push()`、`try_pop()`、`empty()` 使用同一个成员互斥量 `mutex_`。
- `try_pop()` 的判空、取值、删除在一次持锁期间完成。
- `lock_guard` 离开作用域时自动解锁，包括提前 `return` 的情况。
- 空队列返回 `std::nullopt`。
- `empty()` 只反映检查那一刻的状态。

### 我的理解与设计选择

- 判空和出队必须在同一次持锁操作中完成。如果中间释放锁，其他线程可能取走最后一个元素，之前的“非空”判断就失效了。这里讨论的是同一进程内的多个线程。
- 模板实现放在头文件中，不只是为了复用：编译器根据 `ThreadSafeQueue<int>`、`ThreadSafeQueue<std::string>` 等具体类型生成代码时，通常需要看到模板定义。本项目没有采用显式实例化，因此将声明和实现一起放在头文件中。
- `0` 是一个有效的元素值，不能用它代表“没取到任务”。`std::optional<T>` 分别表达“有一个值”和“没有值”，空队列返回 `std::nullopt`。
- `auto` 在声明时推导类型，之后类型不再变化。整数队列的取出结果是 `std::optional<int>`，即使结果为空也仍是这个类型；字符串结果使用单独的变量。
- `push(T task)` 按值接收参数。传入普通字符串变量时，先复制出参数 `task`，再通过 `std::move(task)` 允许队列移走参数的资源，避免再复制一次。`std::move` 本身不搬运数据，实际是否移动以及如何移动取决于类型；对于整数，移动通常没有额外收益。
- `empty()` 只描述检查那一刻的状态，因此调用方应直接根据 `try_pop()` 的结果决定是否使用元素，而不是先调用 `empty()` 来保证随后一定能取到。

### 遇到的问题与解决过程

以下为 Issue #1 学习过程中实际遇到的问题。

- **`std` 中找不到 `mutex`**：补上 `<mutex>` 后仍报错，修改编辑器的 C++17 设置也未解决。直接用编译器检查发现，旧工具链是 GCC 7.3.0 的 `win32` 线程模型，该安装无法编译 `std::mutex`。改用 MSYS2 UCRT64 GCC，并更新编辑器的编译器路径；新版 GCC 的语法检查和后续构建通过。
- **PowerShell 找不到 `g++`**：编辑器的 `compilerPath` 和终端的 `Path` 是不同配置。先用完整路径执行编译器，再将 `C:\msys64\ucrt64\bin` 加入用户 `Path`，重启 VS Code 后验证。最终能直接运行 `g++` 命令。
- **直接编译头文件时出现 `#pragma once` 警告**：头文件被当成主编译文件，导致用途提醒。保留 `#pragma once`，改由测试 `.cpp` 包含头文件并编译；该警告不需要通过删除头文件保护来解决。
- **不会创建 `lock_guard` 对象**：最初只写了类型 `std::lock_guard<std::mutex>;`，没有创建对象或指定互斥量。改为 `std::lock_guard<std::mutex> lock(mutex_);`，让局部对象从构造时加锁，直到离开作用域时解锁。后续构建通过。
- **`empty() const` 中加锁时报参数为 `const std::mutex`**：加锁会改变互斥量的内部状态，而 `const` 成员函数默认不能修改普通成员。将锁声明为 `mutable std::mutex mutex_;`，保留 `empty()` 的 `const`。锁状态可以变化，但队列的逻辑内容不变；修改后编译通过。
- **CMake 的 Ninja 与 MinGW Makefiles 冲突**：原 `build/` 缓存了另一种生成器配置。改用独立的 `build-ninja/`，明确指定新版 GCC、Ninja 和 Debug；配置输出 `Found Threads: TRUE`、`Configuring done`、`Generating done`，随后编译和 CTest 通过。

### 验收与归档

验收重点：单线程存取、至少三个边界测试，以及空结果的处理。以下记录对应 Issue #1 提交时的代码和当时对话中的验证结果，不将后续阶段新增的测试计入本阶段。

- 实际修改的文件：新增 `include/task_queue.hpp`、`tests/task_queue_test.cpp`；修改 `CMakeLists.txt`、`README.md`、`docs/design-notes.md`、`.gitignore`，共六个文件。VS Code 配置用于本地环境设置，没有纳入该提交。
- 验证命令与结果：使用下面的命令完成 Debug 配置、构建和测试。配置识别到 GNU 16.2.0 和线程支持；构建输出 `[4/4] Linking CXX executable task_queue_test.exe`；最后一次反馈的 CTest 结果为 `1/1 Passed`、`100% tests passed out of 1`，总耗时约 0.26 秒。这里的一项测试是一个测试程序，其中执行了多组断言。

  ```powershell
  cmake -S . -B build-ninja -G Ninja -DCMAKE_BUILD_TYPE=Debug -DCMAKE_CXX_COMPILER=C:/msys64/ucrt64/bin/g++.exe -DCMAKE_MAKE_PROGRAM=C:/msys64/ucrt64/bin/ninja.exe
  cmake --build build-ninja
  ctest --test-dir build-ninja --output-on-failure
  ```

- 测试能证明什么、尚未验证什么：测试验证了初始空队列、单个元素 `225` 的存取、取空后再次取出、`10/20/30` 的 FIFO 顺序、有效值 `0` 与空结果的区别，以及字符串 `hello` 的存取和取空。初始为空、取空后再取、零值三个边界均有断言。通过检查实现确认各队列操作使用同一把锁；但本阶段未运行多线程并发测试或数据竞争检测，不能用单线程测试通过来证明所有并发场景正确，也未验证异常路径或仅可移动类型。断言应在未定义 `NDEBUG` 的构建中运行，本次使用 Debug。
- 仍不确定的问题：对话中已解决阻塞构建与测试的问题；后续仍需深入理解移动构造、模板实例化，以及用并发测试验证共享队列的方法。这些是后续学习点，不写成已验证的能力。
- 提交编号或链接：[`eb80b1f`](https://github.com/Zhihui-Cui/cpp-thread-pool/commit/eb80b1f)，提交说明为 `feat: implement generic thread-safe queue (#1)`；该次提交包含六个文件，112 行新增、3 行删除。本次补写的总结不包含在该历史提交中。
- GitHub 推送与 Issue 关闭记录：执行 `git push origin main` 成功，终端显示 `7b79ab3..eb80b1f main -> main`。归档核查时，GitHub Issue #1 的四项验收均已勾选，状态为 `closed`，关闭原因为 `completed`；关闭时间为北京时间 2026-09-11 11:59:35。提交信息中的 `(#1)` 只关联 Issue，本身不是自动关闭指令。

## Issue #2：单线程任务执行器

[查看 Issue](https://github.com/Zhihui-Cui/cpp-thread-pool/issues/2)

### 学习范围与文件

- 学习范围：lambda、捕获、`std::function`、移动语义；提交并按顺序执行任务；头文件与源文件分工、测试组织和 CMake 目标注册。
- 文件位置：`include/single_thread_executor.hpp`、`src/single_thread_executor.cpp`、`tests/single_thread_executor_test.cpp`、`tests/task_queue_test.cpp`、`CMakeLists.txt`；说明与总结写在 `README.md`、`docs/design-notes.md`。

### 我的理解与设计选择

- **保存任务与执行任务是两个动作**：lambda 可以像普通对象一样放进队列。`submit()` 只保存任务，函数体中的赋值或追加元素此时不会执行。`run()` 才负责反复取出任务并调用，直到队列为空。当前执行器不创建新线程，任务在调用 `run()` 的线程中执行。
- **任务需要统一的存储类型**：不同 lambda 有不同类型，`std::function<void()>` 可以包装符合“无参数、无返回值”调用形式的可调用对象，让这些任务进入同一个 `ThreadSafeQueue<std::function<void()>>`。
- **取出不等于调用**：`try_pop()` 将任务从队列移除，并返回包含任务的 `optional`。`has_value()` 检查是否取到任务，`task.value()` 取得函数对象，后面的 `()` 才调用函数。因此 `task.value()()` 才会产生任务中的效果；队列变空可能发生在任务执行之前。
- **捕获决定任务使用哪个数据**：`[&value]` 通过引用访问外部的 `value`；`[&order, i]` 让多个任务访问同一个 `order`，同时让每个任务保存提交时的 `i` 副本。引用捕获的对象必须在任务执行时仍然存活。本阶段的测试在局部变量离开作用域前调用 `run()`。
- **提交时移动任务对象**：`submit(std::function<void()> task)` 按值接收任务，再用 `tasks_.push(std::move(task))` 将参数交给队列。`std::move` 允许后续操作使用移动语义，它本身不会执行任务。
- **封装的目的**：把测试中重复的“入队、循环取出、调用”收进 `SingleThreadExecutor`。调用方使用 `submit()` 和 `run()`，不必自己写取任务循环。`try_pop()` 返回时内部的锁已经释放，调用任务发生在队列锁之外。
- **文件按组件和职责组织**：执行器是普通类，在 `.hpp` 中声明、`.cpp` 中实现。Issue #1 的队列是模板，当前组织方式要求使用它的代码能看到模板定义，因此实现留在头文件中，不需要补建 `task_queue.cpp`。
- **测试按行为拆分**：队列测试验证存取和可调用对象的保存；执行器测试验证延迟执行、顺序执行和不重复执行。每个测试函数创建自己的对象，`main()` 只组织调用，避免测试之间依赖同一个队列的残留状态。
- **CMake 描述构建关系**：将执行器源文件加入 `thread_pool` 库；用 `add_executable` 创建测试程序、`target_link_libraries` 链接库、`add_test` 注册测试。终端中的配置、构建和测试是不同步骤，`ctest` 不会重新编译修改后的代码。

### 遇到的问题与解决过程

- **`value == 42` 断言失败**：最初只写了 `task.value();`，取到函数对象却没有调用它，所以 `value` 仍为初始化的 `0`。改成 `task.value()();`，重新编译和测试后通过。由此理解“保存、取出、执行”是三个不同操作。
- **测试不断堆进一个 main**：最初整数、字符串、lambda 和顺序执行练习都写在队列测试中。整理时把队列测试拆成六个独立函数，执行器测试拆成两个独立函数，并去掉队列测试中重复的三个 lambda 顺序执行练习。整理后两个测试程序均通过。
- **不清楚新组件如何加入工程**：起初只理解了任务调用，没有把文件结构和构建步骤联系起来。随后创建执行器头文件、源文件与独立测试，将源文件加入库并注册测试目标。CMake 按“项目配置、库、测试”整理；VS Code 的 C++ 标准也从 C++20 对齐为实际构建使用的 C++17。

### 验收与归档

验收重点：能提交 lambda、按提交顺序执行，以及最小示例可运行。

- 实际修改的文件：提交新增执行器头文件、源文件和测试文件；修改 `CMakeLists.txt`、`tests/task_queue_test.cpp`、`README.md`、`docs/design-notes.md`，共七个文件。VS Code 配置调整属于本地环境设置，未包含在该提交中；提前创建的线程池测试和 benchmark 空文件也未纳入本阶段提交。
- 验证命令与结果：本阶段完成配置、构建与 CTest 验证。整理代码后的测试为 `2/2 Passed`；收尾核查时再次构建并运行测试，结果为 `100% tests passed out of 2`，总耗时约 0.27 秒。以下记录来自当时实际执行结果，本次补写文档未重新运行测试。

  ```powershell
  cmake -S . -B build-ninja
  cmake --build build-ninja
  ctest --test-dir build-ninja --output-on-failure
  ```

- 测试能证明什么、尚未验证什么：队列测试验证函数对象可以保存、取出，并在显式调用后修改外部变量；执行器测试验证提交时结果容器仍为空，`run()` 后结果为 `{1, 2, 3}`，以及再次运行不会重复执行已完成任务。执行器测试本身是可运行的最小使用示例，README 另附打印任务的用法。CTest 的“两项”对应两个程序，内部有多个测试函数。本阶段未验证多线程执行、并发提交、任务异常、空函数对象或仅可移动任务，也没有实现 future 和关闭语义；单线程测试通过不能证明未来线程池的并发正确性。当前验证使用 Debug，保证断言有效。
- 仍不确定的问题：后续继续深入移动语义与模板实例化，并学习 worker 生命周期、共享数据同步、future 和异常传递。本阶段出现的函数未调用问题已解决，后续主题不作为当前已掌握或已验证的能力。
- 提交编号或链接：[`6cf0b37`](https://github.com/Zhihui-Cui/cpp-thread-pool/commit/6cf0b37d0052c8e143154568a6b65c714c75ff7f)，提交说明为 `feat: add single-thread task executor`，正文为 `Refs #2`；提交时间为北京时间 2026-09-13 22:58:57，共七个文件，567 行新增、73 行删除。本次补写的总结不包含在该历史提交中。
- GitHub 推送与 Issue 关闭记录：2026-09-14 归档核查时，远端 `main` 与本地 `main` 均指向 `6cf0b37`，确认该提交已在 GitHub。Issue #2 的三项验收均已勾选，状态为 `closed`，关闭原因为 `completed`；关闭时间为北京时间 2026-09-13 23:00:04。`Refs #2` 用于关联 Issue，本身不是自动关闭指令。

## Issue #3：多 worker 生命周期管理

[查看 Issue](https://github.com/Zhihui-Cui/cpp-thread-pool/issues/3)

### 学习范围与文件

- 学习范围：`std::thread`、共享队列、`join()`、停止状态与作用域；指定 worker 数并正确完成 1,000 个任务；补充 `explicit`、函数包装、异常捕获与重新抛出、构建链接和代码风格。
- 主要文件：`include/thread_pool.hpp`、`src/thread_pool.cpp`、`tests/thread_pool_test.cpp`、`CMakeLists.txt`；文档和格式约定见下方归档记录。

### 我的理解与设计选择

以下内容依据本阶段本人在对话中的复述整理，并保留纠正后的表述；不把经过提示后能够解释等同于已经能独立重写全部代码。

- **主线程创建 worker，新线程执行入口函数**：`workers_` 是存放 `std::thread` 对象的容器，不是一个线程。`emplace_back([this] { worker_loop(); })` 在容器中构造线程对象，由新线程调用 lambda，再进入当前线程池对象的 `worker_loop()`。主线程不等待这个循环结束，因此可以继续创建其他 worker，并在构造函数返回后提交任务。直接调用 `worker_loop()` 则仍在当前线程中执行，不会自动创建线程。

- **提交和执行由不同代码负责**：`submit()` 把任务移入 `tasks_`；worker 从队列取出函数对象，调用 `task.value()()` 才执行任务。后台线程已启动，任务可能在 `submit()` 返回前开始执行。
- **函数包装描述调用方式**：`std::function<void()>` 表示无参数、调用时不提供返回值的形式。`std::function<int()>` 可返回整数，但 worker 拿到结果不等于主线程自动得到结果；当前 `submit()` 返回 `void`，跨线程获取结果留到 Issue #5。`explicit` 限制整数隐式转换为线程池对象，仍允许 `ThreadPool pool(4)` 这样的直接构造。
- **两把成员锁各有职责**：队列的 `mutex_` 保护判空、取出和删除；线程池的 `state_mutex_` 保护 `stopping_` 的访问，当前实现还将取任务放在这一临界区中。队列锁在 `try_pop()` 返回时释放，状态锁在内部作用域结束时释放。调用任务时不持有这两把锁。外层状态锁目前也串行化了 worker 的取任务过程，是本阶段便于理解的保守实现。
- **任务自身的共享数据另行保护**：队列安全不等于任务修改的数据也安全。多个 worker 执行 `++count` 时，要使用同一把锁；各自创建一把锁不能形成互斥。测试使用 `result_mutex` 保护 `hits`，每个任务按值捕获自己的编号 `i`，引用捕获的结果容器和锁必须存活到任务结束。
- **空队列不等于任务完成**：一个 worker 可能已经取走最后的任务但还在执行。取不到任务且 `stopping_ == false` 时继续轮询；取不到任务且 `stopping_ == true` 时才退出。即使已请求停止，取到了任务仍先执行。`yield()` 只是建议调度器给其他线程执行机会，返回后继续检查，不是睡到新任务到来，也不保证一定切换线程。
- **析构先通知，再等待**：当前用法中主线程执行析构函数，加锁设置 `stopping_ = true`，解锁后逐个 join。若持锁 join，worker 无法取得同一把锁来检查退出条件，双方可能互相等待。`join()` 等待线程结束，不启动线程，也不强制中断正在执行的任务。
- **构造失败需要就地清理**：第三个 worker 创建失败时，前两个可能已经运行，但整个线程池尚未构造成功。构造函数抛出异常时不会调用 `~ThreadPool()`；已构造好的成员仍会被销毁。因此构造函数的 `catch (...)` 先设置停止状态、解锁、join 已创建的线程，再用 `throw;` 保留并重新抛出原异常。如果捕获后清理但不重新抛出，构造函数会正常结束，对象会被视为构造成功，却没有仍在工作的 worker；容器中可能还保留已经 join 的线程对象。不能简单说成“对象没有创建”。

### 遇到的问题与解决过程


- **头文件找不到**：手动编译命令漏了 `-I include`；补充头文件搜索路径后继续编译。
- **`undefined reference` 链接错误**：手动命令只有测试源文件，没有加入 `src/thread_pool.cpp`。头文件声明不会自动把同名源文件加入构建；一起编译后链接成功。随后把新测试注册进 CMake，复用已有 `thread_pool` 库。
- **声明和定义混淆**：头文件曾写空的构造、析构函数体，源文件又使用没有类名前缀的构造写法。头文件改为分号结尾的声明；类外实现使用 `ThreadPool::ThreadPool(...)`，`explicit` 保留在类内声明处。
- **不清楚局部大括号的作用**：通过对比持锁范围，理解 `lock_guard` 离开作用域才解锁；空行只是排版，不能代替作用域。任务执行和 join 都放在状态锁的作用域之外。
- **异常语法与传播不熟悉**：将非法的 `catch()` 改为 `catch (...)`；将清理后的新 `invalid_argument` 改为 `throw;`，避免丢失创建失败的原异常类型和信息。参数为零时仍主动抛出 `invalid_argument`。



### 验收与归档

验收重点：worker 数可配置、1,000 个任务全部正确完成、共享队列无数据竞争，以及创建与退出逻辑清晰。

- 实际修改的文件：实现 `include/thread_pool.hpp`、`src/thread_pool.cpp`；新增 `tests/thread_pool_test.cpp` 并在 `CMakeLists.txt` 注册；更新 `README.md`、本文件；新增格式配置和 `docs/coding-style.md`，整理已有队列与执行器代码的排版。本地 `.vscode/settings.json` 按原有忽略规则不提交，空的 benchmark 文件不是本阶段成果。
- 验证命令与结果：2026-09-15 汇总前确认构建类型为 Debug；执行以下命令，构建输出 `ninja: no work to do.`，CTest 为 `3/3 Passed`，总耗时约 0.22 秒。格式统一时另已通过 clang-format 检查与 `git diff --check`。以下 30 秒是单个测试程序的超时上限，不是用于等待任务完成的睡眠。

  ```powershell
  cmake --build build-ninja
  ctest --test-dir build-ninja --output-on-failure --timeout 30
  ```

- 已验证的行为：线程基础练习、4 个 worker 共同消费预先填好的任务队列；线程池分别使用 1、2、4 个 worker，每次提交 1,000 个任务，析构后逐个检查 `hits[i] == 1`；零 worker 被拒绝；不提交任何任务的线程池能正常析构。`thread_pool_test` 内部包含多个函数，CTest 总数仍为三个程序。
- 同步检查与测试边界：通过代码检查确认共享队列操作使用同一把队列锁，停止状态使用同一把状态锁，测试结果另行加锁，执行任务和 join 均不持有队列锁或状态锁。测试没有运行数据竞争检测器，不能证明所有并发调度都正确；未验证任务平均分配或每个 worker 都实际执行过任务，也未做性能比较。
- 尚未验证的异常路径：构造中途创建线程失败时的回收和重新抛出，仅做代码检查，未通过故障注入触发。任务异常、空函数对象、并发提交者、提交与析构的竞争，以及反复创建销毁等场景未覆盖。当前任务应能正常返回，提交应在线程池析构开始前结束，捕获的对象应保持存活；不能在线程池自身的 worker 中销毁该线程池。
- 仍需巩固：独立重写构造失败清理，区分“抛出新异常”和“重新抛出原异常”，以及如何用可控测试验证异常路径。阻塞等待、future 和完整关闭接口仍按 Issue #4～#6 推进。
- 提交编号或链接：[`466f88b`](https://github.com/Zhihui-Cui/cpp-thread-pool/commit/466f88bc0a431f1e8e710e8151c707282d485434)，提交说明为 `feat: implement multi-worker thread pool lifecycle`，正文为 `Refs #3`；提交时间为北京时间 2026-09-15 23:03:22，包含 12 个文件，481 行新增、47 行删除。本次补写的提交与关闭记录不包含在该功能提交中。
- GitHub 推送与 Issue 关闭记录：2026-09-16 归档核查时，通过 `git ls-remote origin refs/heads/main` 确认远端 `main` 指向完整提交 `466f88bc0a431f1e8e710e8151c707282d485434`，与本地一致。Issue #3 的四项验收均已勾选，状态为 `closed`，关闭原因为 `completed`；关闭时间为北京时间 2026-09-16 15:43:22。[验收评论](https://github.com/Zhihui-Cui/cpp-thread-pool/issues/3#issuecomment-5693848715) 已记录实现、测试、提交编号和未覆盖的验证范围。`Refs #3` 仅关联 Issue，本身不会自动关闭它。本次归档只修改文档，沿用上方已有测试证据，未重复运行测试。

## Issue #4：阻塞等待与唤醒

[查看 Issue](https://github.com/Zhihui-Cui/cpp-thread-pool/issues/4)

### 学习范围与文件

- 学习范围：condition_variable、unique_lock、等待谓词、虚假唤醒。
- 文件位置：`include/thread_pool.hpp`、`src/thread_pool.cpp`、`tests/thread_pool_test.cpp`；文档为 `README.md` 和本文件。沿用已有队列接口。

### 我的理解与设计选择

以下内容依据本阶段本人在对话中的复述与追问整理，并保留纠正后的表述；在提示下完成实现和解释，不等于已经能够独立重写全部代码。

- **从轮询改为阻塞等待**：原来的 worker 在没有任务时调用 `yield()`，随后继续循环检查队列，仍然消耗 CPU 执行这些检查。改用带谓词的 `wait()` 后，worker 在队列为空且没有停止请求时释放锁并阻塞等待。被唤醒后，它重新取得锁并检查状态，再决定继续等待、取任务或退出。`wait()` 刚被调用时也会先检查谓词，条件成立便直接返回，不必先等待一次通知。
- **对象和执行线程的区别**：`cv_` 是线程池的条件变量成员，不是线程，也不会自己执行代码。worker 调用 `cv_.wait()`，等待的就是该 worker；提交线程调用 `cv_.notify_one()`。测试里的 `result_cv` 是另一个对象，由主线程等待、任务通知；它与线程池内部的 `cv_` 各自管理等待者。
- **锁与等待的顺序**：`unique_lock` 创建时取得 `state_mutex_`。`wait()` 在持锁状态下检查谓词；为假时原子地解锁并进入等待；醒来后先重新取得锁，再检查谓词。等待期间局部 `lock` 对象仍存在，但不持锁。谓词为真时 `wait()` 返回，仍持有锁；取任务后离开内部作用域，再执行任务。
- **为什么传匿名函数**：直接传 `stopping_ || !tasks_.empty()` 只会先计算出一个 `bool`，不符合谓词需要可调用对象的接口要求。lambda 可供 `wait()` 在进入等待前、每次醒来后重新调用。lambda 的 `return` 把判断结果交给 `wait()`，不会退出 `worker_loop()`；一次带谓词的 `wait()` 调用内部可以反复等待和判断，直到条件成立。
- **结束等待与退出不同**：谓词 `stopping_ || !tasks_.empty()` 表示有任务或需要处理停止请求。即使已经停止，取到任务仍先执行；只有 `!task.has_value() && stopping_` 成立才退出循环。停止请求不强制中断正在执行的任务。
- **两把锁的配合**：提交线程也会通过 `tasks_.push()` 使用队列内部的锁，但这把锁只保护单次队列操作，不能覆盖检查条件到进入等待的配合过程。因此 `submit()` 还取得 `state_mutex_`，入队、解锁后通知；worker 使用同一把外层锁检查条件并等待。这样避免任务在检查为空后、进入等待前入队并发出无法保留的通知。当前需要两把锁时，顺序均为先状态锁、再队列锁。
- **通知不分配任务**：`notify_one()` 通知同一条件变量上的一个等待线程，不保证先等待者先被唤醒，也不把锁或任务交给它。另一个正在执行旧任务的 worker 可能先回到循环、取得锁并取走新任务。虚假唤醒时也未必有任务，所以醒来后必须重新检查谓词。接口规则参考 [C++ 标准草案](https://eel.is/c++draft/thread.condition.condvar)。
- **停止时先解锁、通知，再 join**：析构和构造失败清理都先在锁内设置 `stopping_`，解锁后 `notify_all()`，再逐个 `join()`。先 join 可能使通知无法执行；持锁 join 会让 worker 无法取得锁检查退出条件；只通知一个可能留下其他等待者。`join()` 只等待结束，不负责唤醒或停止线程。
- **异常处理需要自己编写**：`try` 中调用的标准库操作也可能抛出异常。`catch (...)` 捕获从对应 try 块传播出的各种 C++ 异常，清理动作由自己编写；它不会捕获另一个 worker 线程执行任务时抛出的异常。构造失败时已创建的 worker 不会自动结束，清理后用 `throw;` 重新抛出原异常，向调用方报告失败；不能清理完就把构造当作成功。
- **析构前检查与重复测试**：析构本身会通知 worker，只在析构后检查结果，无法区分任务是否依靠析构通知才执行。新测试在同一个单 worker 池存活期间执行三轮，每轮加锁重置 `completed`、提交任务，再由主线程使用带谓词的 `wait_for()` 等待标记并断言。任务加锁设置标记、解锁后通知；若不重置，上一轮的 `true` 可能使下一轮直接通过。两秒是等待时限，条件满足可提前返回；等待返回的 `bool` 表示最后的谓词判断结果。

### 遇到的问题与解决过程


- **把通知写在 join 后面**：析构和构造函数 catch 中最初均将 `notify_all()` 放在 join 循环后。代码检查发现等待中的 worker 可能无法退出，主线程也就执行不到通知。将通知移到解锁之后、join 之前，再构建和运行测试通过；没有把错误版本运行挂住当作已发生的测试结果。
- **旧的 yield 分支未删除**：第一次加入等待后仍保留原来的 else/yield，检查后删除，统一由谓词等待处理空闲状态。
- **重置完成标记时遗漏锁**：三轮测试最初直接写 `completed = false`，之后补上独立的 `lock_guard` 作用域，并保留声明时初始化。当前成功等待后上一轮已完成对标记的写入，不能直接把原写法断言为数据竞争；统一加锁使共享状态访问规则更明确。

### 验收与归档

验收重点：空闲时不持续轮询、有新任务时唤醒 worker、使用谓词等待，以及记录设计选择。

- 实际修改的文件：本阶段修改线程池头文件、源文件、线程池测试、README 和本文件，共五个文件；队列接口与 CMake 不需要改动。工作区原有的其他文件改动不计作本阶段成果。
- 验证命令与结果：2026-09-17 在补齐每轮重置的锁后执行 `cmake --build build-ninja`，线程池测试重新编译并链接成功；执行 `ctest --test-dir build-ninja --output-on-failure --timeout 30`，结果为 `3/3 Passed`，总耗时约 0.51 秒。构建目录使用 Debug。以上是实际执行结果；之后整理笔记未修改代码，也未重复运行测试。
- 已验证的行为：沿用的队列、执行器和线程池测试通过；新增测试使用同一单 worker 池，连续三轮在析构前观察到任务完成标记。CTest 的三项仍是三个测试程序，不是三个任务或三个断言。
- 同步检查与测试边界：代码检查确认空闲路径使用谓词等待，无 yield 轮询；入队后 notify_one，停止清理中解锁、notify_all 后 join。测试没有保证提交前 worker 已进入阻塞，不能确定性检出所有漏通知情况；完成标记也不代表任务函数已经完全返回。未测量空闲 CPU，未主动制造虚假唤醒，未运行数据竞争检测器，也未注入构造中途创建失败；异常清理路径仅做代码检查。沿用 Issue #3 的使用约束：任务正常返回，提交在析构开始前结束，引用捕获对象存活至任务结束，不在池自身的 worker 中销毁线程池。
- 仍需巩固：独立重写等待与通知流程、构造失败清理，以及使用可控同步验证 worker 确已等待后的唤醒；不把引导下复述等同于独立掌握底层实现。
- 提交编号或链接：[`b7b9ac2`](https://github.com/Zhihui-Cui/cpp-thread-pool/commit/b7b9ac2cadaa4665b4a10fbbe4f80db4a83b99ca)，提交说明为 `feat: add blocking worker wait and wakeup`，正文为 `Refs #4`；包含五个文件，86 行新增、22 行删除。提交前 `git diff --cached --check` 通过。本次收尾未找到可调用的 clang-format，因此没有声称通过格式工具检查；代码沿用已有排版。本条归档信息通过后续文档提交补记。
- GitHub 推送与 Issue 关闭记录：2026-09-17 执行 `git push origin main` 成功，远端从 `d2ba25d` 更新至 `b7b9ac2`。GitHub Issue #4 的四项验收已全部勾选，正文已记录提交、测试与未覆盖范围，状态为 `closed`，关闭原因为 `completed`；关闭时间为北京时间 2026-09-17 16:47:30。`Refs #4` 仅关联 Issue，本次通过更新 Issue 状态完成关闭。本次归档沿用已有测试证据，未修改代码或重复运行测试。

## Issue #5：submit 与 future

[查看 Issue](https://github.com/Zhihui-Cui/cpp-thread-pool/issues/5)

### 学习范围与文件

- 学习范围：future、packaged_task、共享状态、异常传递、智能指针与移动语义、函数模板与返回类型推导；随后扩展到初始化捕获、mutable lambda、参数包、tuple 和 apply。
- 功能文件：`include/thread_pool.hpp`、`src/thread_pool.cpp`、`tests/thread_pool_test.cpp`；文档为 `README.md` 和本文件。队列、worker 等待循环和 CMake 的测试注册方式沿用前一阶段。

### 我的理解与设计选择

以下记录依据本阶段对话中的复述、纠正后的解释，以及实际实现整理。

- **提交与计算是两次不同的调用**：主线程调用 `submit()` 创建和保存任务，worker 从队列取出后调用它。任务内部的 `return` 不会自动变成主线程那次 `submit()` 的返回值。只取出函数对象而不调用，不会执行计算。
- **future 提供结果获取途径**：`packaged_task` 和 `future` 关联同一个共享状态。worker 调用包装任务后，返回值或异常被保存到共享状态；主线程通过 `get()` 等待并获取。`get_future()` 只取得关联的 future，不执行任务，也不等待计算。worker 先完成、主线程稍后再 get，结果不会因为当时没人等待而丢失。
- **等待与线程分工**：若只有主线程，并把 `get()` 放在它自己执行 `task()` 之前，就会等不到任务结果。若任务已交给独立的 worker，主线程可以等待。`submit()` 不等待计算，不代表它一定先于计算完成返回；两种先后顺序都允许。
- **`get()` 和 `join()` 等待的对象不同**：前者等待任务结果，后者等待线程结束。worker 提供结果后可能继续执行其他任务，不会因此退出。普通 future 的 `get()` 只能获取一次，即使那次 get 重新抛出了任务异常，也不能继续使用同一个 future 获取下一个任务的结果。
- **异常在两条线程中分别抛出**：任务在 worker 中执行 `throw` 后，不再执行后面的普通返回语句；外层 `packaged_task` 捕获并保存异常。主线程调用 `get()` 时重新抛出，再由主线程的 `catch` 处理。`std::apply` 只负责调用，不保存异常。仅包住提交操作、没有 get 的 catch，不能收到已经由包装任务保存的任务异常。
- **任务对象的生命周期**：辅助函数中的局部包装任务不能只用引用交给队列，因为函数返回后局部对象就会销毁。`make_shared` 返回管理包装任务的 `shared_ptr`；`auto task` 因这个返回类型成为智能指针，并非 lambda 捕获自动产生智能指针。入队的 `[task]` 保存智能指针副本，让包装任务在执行时继续存活。
- **两层任务的职责**：队列仍存 `std::function<void()>`；worker 调用外层 lambda，lambda 再调用 `(*task)()`。外层无需返回整数，包装任务通过共享状态交付结果。复制外层 lambda 只复制 shared_ptr，不复制内部不可复制的用户任务，因此符合 `std::function` 对可复制目标的要求。
- **捕获与调用参数不同**：`[add_two]` 在创建 lambda 时保存任务对象，`add_two(40)` 才是调用并传入整数。按值捕获整数会保存当时的副本；主线程修改原变量不会影响副本。引用捕获访问同一个原变量，需要保证生命周期和必要同步。双方正确加锁也只保证访问不冲突，不保证一定读取提交时的旧值。
- **移动不等于执行**：`std::move` 允许后续构造或赋值转移资源，本身不调用任务。初始化捕获 `saved_function = std::move(function)` 在主线程创建 lambda 时初始化内部对象；转交整个 lambda 时同样使用 `std::move(lambda)`。`unique_ptr` 不可复制，保存它的 lambda 也不可复制；测试通过移动捕获和移动提交保存整数 40，再由 worker 加上传入的 2。
- **模板与类型推导**：`F` 是编译器确定的任务对象类型，不是任务返回值。`decltype(function())` 分析调用的返回类型，不执行任务；`using ReturnType = ...` 定义类型别名。后置返回类型可以使用前面声明的参数名。模板函数体放在头文件中，让调用方编译时能根据实际类型实例化；并非让 worker 在运行时读取头文件。
- **正式接口的分工**：模板 `submit()` 负责包装任务并返回 future；私有 `enqueue()` 负责锁内入队、解锁后通知。调用 `pool.submit()` 时，`this` 指向具体的 pool 对象，使用 `this->enqueue()` 访问成员。带参数版本将任务与参数保存进无参数 lambda，再调用无任务参数版本，不重复编写包装和入队逻辑。
- **mutable 与初始化捕获**：lambda 中的 `mutable` 允许修改按值捕获的对象，不会变成引用捕获，也不提供互斥。带参数包装使用 mutable，才能调用需要修改自身捕获状态的任务。初始化捕获左边是内部的新名字，右边使用已有对象进行初始化。
- **多参数保存与执行**：`Arg argument` 接收第一个任务参数，`Args... args` 接收剩余参数；`std::move(args)...` 对每个后续参数分别使用 move。主线程用 `make_tuple` 保存它们，worker 执行 lambda 中的 `std::apply` 时展开 tuple，并调用一次计算任务。tuple 是数据对象，apply 不创建线程。
- **无返回值任务仍有完成状态**：`future<void>::get()` 不提供数值，但会等待完成或重新抛出异常。`return std::apply(...)` 在表达式类型为 void 时可以用于结束无返回值的包装调用。单个任务写外部变量后，主线程成功 get 再读取有同步保证；去掉等待后偶尔断言通过，不能证明代码没有数据竞争。

### 遇到的问题与解决过程


- **迁入成员函数时接口不完整**：拆出 `enqueue()` 时曾删除普通 `submit()` 的实现却仍保留声明；迁入模板后，又遗漏删除旧的 `.cpp` 定义。按阶段补齐或移除对应定义。成员模板中残留 `pool.enqueue`、随后误写 `this.enqueue`，最终改为 `this->enqueue`。这些问题在代码检查时指出，不记录为已经实际运行的编译失败。

- **返回类型验证不充分**：仅使用 `double value = result.get()` 不能证明 result 是 `future<double>`，因为 int 可以转换为 double；改用显式的 `std::future<double>` 接收。`future<int>` 本身不能转换为 `future<double>`。
- **后续任务取错 future**：异常测试提交第二个正常任务后，曾再次调用第一个 `result.get()`。改用第二个任务的 `next_result.get()`；在同一个单 worker 池中得到 42，验证原 worker 在任务异常后仍可工作。新建线程池不能验证这一点。
- **异常测试可能误通过**：只在 catch 内写断言无法发现“没有异常”的情况，因此使用初始为 false 的标记，在 try/catch 外断言。多参数异常测试曾使用 `catch (...)`，会将其他错误也算作成功；改为捕获预期的 `std::runtime_error`。最终代码已移除异常测试中无用的结果局部变量。
- **捕获与共享数据混淆**：按值捕获使用独立副本，不需要为主线程修改原整数而加锁。改成引用捕获则需同步；加锁也不能规定谁先读取或修改。void 任务测试先 get 再断言，避免把异步写入与主线程读取放在缺少同步的路径中。

### 验收与归档

验收重点：返回 42 的任务可通过 get() 获取结果、支持带参数或捕获的 lambda，以及任务异常能从 future 取回。

- 本阶段文件：功能代码为 `include/thread_pool.hpp`、`src/thread_pool.cpp`、`tests/thread_pool_test.cpp`；说明为 `README.md` 和本文件。本次文档整理未替用户修改实现。工作区中的 `include/single_thread_executor.hpp` 改动和未跟踪 benchmark 文件不纳入本阶段整理范围。
- 最新构建与测试：2026-09-21 确认使用 Debug，执行 `cmake --build build-ninja`，输出 `ninja: no work to do.`；随后执行 `ctest --test-dir build-ninja --output-on-failure --timeout 30`，结果为 `3/3 Passed`，总耗时约 0.65 秒。此前新增测试已实际重新编译并运行；本条是整理文档前对当前文件的核验。CTest 的 3 指三个测试程序。
- 文档示例验证：从 README 的“返回值与异常”章节原样提取完整示例到 `build-ninja/readme_issue5_example.cpp`，使用当前 UCRT64 g++、`-std=c++17 -Wall -Wextra -Wpedantic` 和已有线程池库编译；程序在启用断言的情况下运行成功，验证整数、多参数、void 和异常示例。文档差异通过 `git diff --check`；未运行 C++ 格式化工具。

| 验收内容 | 实际证据 |
| --- | --- |
| 提交任务后通过 get 得到 42 | `test_submit_provides_int_result` 直接调用正式 submit 并断言 42；拆开 submit/get，等价验证验收示例中的结果路径 |
| 捕获或带参数 lambda 可运行 | 值捕获保留提交时的 40；单参数 40 加 2 得到 42；多参数 20、5、0.5 按顺序计算得到 15.5 |
| 任务异常从 future 取回 | 无参数与带参数任务均通过 get 捕获 `std::runtime_error`，并在 catch 外检查标记；无参数异常测试继续使用同一单 worker 池执行正常任务，得到 42 |
| 提供对应测试 | 上述测试均在 main 中调用，编译并由 CTest 执行；保留基础练习作为执行流程示例 |

- 额外已验证行为：明确的 `future<double>` 和 `future<void>` 类型；带参数 void 任务完成后可观察到 42；mutable 任务修改自身副本且外部 count 保持 0；捕获 unique_ptr 的不可复制任务通过移动提交得到 42。旧的队列、单线程执行器、1/2/4 worker 各执行 1,000 个任务、析构和唤醒测试也通过。
- 测试边界：未固定 worker 在 submit 返回前或返回后执行，不能声称已经确定性验证“局部提交变量销毁后才开始执行”；未证明 get 必然阻塞。未检查异常消息、所有异常类型、并发提交者、任务公平分配、数据竞争检测、构造线程失败注入或性能。既有同步与构造清理限制仍见 Issue #3、#4。
- 接口边界：参数按值接收并保存在 tuple 中，随后以保存元素的左值形式传给计算任务；尚不支持按值向计算任务转交保存的 unique_ptr 参数。这与已测试的不可复制任务对象是不同场景。引用返回值、成员指针和所有引用包装组合未验证；没有将接口描述为完整的完美转发或通用调用实现。
- 后续技术问题：future 与 packaged_task 的共享状态底层实现未复现；返回局部 future 时的消除复制/移动规则未系统展开；参数保存与再次转交所有权的区别可在后续专项学习。引用捕获仍需保持对象存活，提交仍应在析构前结束，公开关闭协议留到 Issue #6。
- 提交编号或链接：[`81ed617`](https://github.com/Zhihui-Cui/cpp-thread-pool/commit/81ed6170f79473fe688f4a3b874e6702b439cb81)，标题为 `feat: add future-based task submission`，正文使用 `Refs #5` 关联 Issue；包含上述五个阶段文件，445 行新增、30 行删除。本条提交和关闭记录由后续文档提交补记。
- GitHub 推送与 Issue 关闭记录：2026-09-21 推送 main 成功，远端从 `0f59f5c` 更新至 `81ed617`，随后通过 `git ls-remote` 核对远端 main 指向完整功能提交编号。首次直连 GitHub 失败，改为在该次 Git 命令中使用系统已有的本地代理后完成推送，未修改持久 Git 配置。Issue #5 的四项验收已全部勾选，正文记录提交、测试与限制，状态为 `closed`，原因为 `completed`；关闭时间为北京时间 2026-09-21 19:58:02。`Refs #5` 本身不自动关闭 Issue，本次通过更新 Issue 状态完成关闭。归档只修改文档，沿用上述验证结果，未重复运行测试。

## Issue #6：优雅关闭

[查看 Issue](https://github.com/Zhihui-Cui/cpp-thread-pool/issues/6)

### 学习范围与文件

- 学习范围：停止状态、拒绝新任务、完成已提交任务、唤醒与 join、析构。
- 文件位置：线程池实现、关闭测试及 README 的关闭语义说明。

### 我的理解与设计选择

以下记录依据本阶段对话中的复述、代码修改和纠正后的理解整理。复杂同步测试是在分步引导下完成的，不将能够解释等同于已经能够独立设计和重写。

- **停止请求不等于立即退出**：`stopping_ = true` 后，worker 取到任务仍继续执行；只有取不到任务且已经停止时才退出。单 worker 正在执行 A、队列还有 B/C 时，A 返回后会继续执行 B/C，再退出。若仅检查 `stopping_` 就退出，会遗漏任务；当前判断在 `try_pop()` 之后，甚至可能已经取出一个任务却没有执行。
- **检查状态与入队必须连续持锁**：`enqueue()` 在 `state_mutex_` 的同一次持锁期间检查停止状态并入队。如果提交线程先成功入队，任务随后仍会执行；如果关闭线程先设置停止状态，提交会抛出 `std::runtime_error`。不能先检查再另行加锁入队，否则可能在 worker 已退出后才加入任务，队列不会自动识别或拒绝它。普通 bool 的并发读写也需要同步。
- **拒绝提交与任务异常发生在不同位置**：停止后的异常直接从 `enqueue()` 经 `submit()` 传给提交方，本次调用不返回 future；任务执行中的异常则由 `packaged_task` 保存，在调用方 `get()` 时重新抛出。抛异常离开作用域时，局部锁对象会析构并解锁。
- **关闭顺序是设置状态、解锁、通知、join**：`stop()` 在锁内设置停止标志，然后解锁并 `notify_all()`，最后 join 所有 worker。持有状态锁等待 join，会阻止 worker 取得同一把锁检查退出条件，形成相互等待。join 不负责通知或强制终止任务；任务不能结束时，关闭也不能完成。
- **依次重复关闭与并发关闭不同**：析构调用公开的 `stop()`，每次 join 前检查 `joinable()`，跳过已经 join 的线程。joinable 不表示线程仍在运行，已结束但未 join 的线程仍可 join。这支持同一线程依次重复关闭，不支持多个线程同时操作同一组线程对象。停止后的池不重启，反复启动关闭测试每轮创建新池；循环中的 continue 也会触发本轮局部对象析构。
- **不能让 worker 等待自己结束**：在池自身的任务中调用会 join 所有 worker 的 stop，会尝试 join 当前线程。复述时识别出了“自己等待自己”的循环依赖；实际自身 join 会抛出 `std::system_error`，不能把这种调用当作支持的关闭方式。析构前外部调用应已结束，任务引用的数据应保持存活。
- **锁对象存活不等于一直持锁**：`lock_guard` 用于整个局部作用域持锁；`unique_lock` 还允许手动解锁、重新加锁，并可传给条件变量。带谓词的 wait 在持锁时检查条件，为假则原子地解锁并等待，醒来重新加锁、再次检查；条件成立返回时仍持锁。notify 不修改标记，也不释放调用方持有的锁。future 的 get 不会替调用方释放其他互斥量。
- **等待条件与等待时限**：带谓词的 wait 等到条件成立；带谓词的 wait_for 还接受时限，返回 bool 表示最终条件是否成立，不是固定睡满指定时间。谓词处理虚假唤醒，并在通知早于等待时通过已保存的状态判断是否还需要等待。
- **控制关闭测试的时序**：单 worker 的 A 先设置 started，再等待 release；主线程确认 A 开始后提交 B/C，由独立 closer 线程调用 stop。主线程尝试提交空任务，观察到拒绝后才放行 A，从而确认停止状态生效时 B/C 仍在排队。创建 closer 本身不能证明 stop 已执行。探测中不能 get 排队任务，否则主线程等任务、任务等 A、A 等主线程放行，会再次形成死锁。探测有两秒时限，结束后先放行、join，再断言，整个程序另由 CTest 超时保护。

### 遇到的问题与解决过程

- **A 的等待测试未成功运行**：本人反馈编译运行后没有成功运行。代码检查发现主线程持有 gate_mutex 时调用 result.get，A 无法取得或重新取得锁来完成；同时 A 使用无谓词 wait，主线程也没有等待 started。改为双方使用对应谓词，主线程通过局部作用域在通知和 get 之前解锁。后续包含该测试的 CTest 运行通过；未将原始失败描述为已有具体报错或超时日志。
- **混淆 wait 返回与等待期间的锁状态**：最初以为谓词为 true 会解锁、false 则一直持锁。通过按线程推演，纠正为等待期间解锁、返回时持锁；理解 unique_lock 对象仍存活时可以暂时不持锁。
- **不会编写关闭状态探测**：最初不理解为什么需要截止时间、循环提交和 yield。分步解释后理解这是观察停止状态，yield 不保证关闭线程已运行，提交被拒绝才是证据。该局部实现由教学示例提供，并非独立设计完成。

### 验收与归档

验收重点：停止后不接收新任务、已提交任务按约定完成、所有 worker 被唤醒并 join、反复启动关闭不崩溃或死锁，以及文档说明。2026-09-22 对照下表完成本地验收；实现、测试与文档在当前声明的使用范围内一致，随后完成提交、推送和 GitHub 归档。

- 本阶段文件：`include/thread_pool.hpp`、`src/thread_pool.cpp`、`tests/thread_pool_test.cpp`、`README.md`、本文件。工作区原有的 `include/single_thread_executor.hpp` 改动及未跟踪 benchmark 不纳入本阶段范围。
- 最近实际验证：2026-09-22 执行 `cmake --build build-ninja`，输出 `ninja: no work to do.`；执行 `ctest --test-dir build-ninja --output-on-failure --timeout 30`，三个测试程序全部通过，总耗时约 0.26 秒。本次整理笔记只修改文档，沿用该次运行证据。

| 验收内容 | 当前证据 |
| --- | --- |
| stop 后拒绝提交 | `test_submit_rejected_after_stop` 捕获预期异常，并在 catch 外断言 |
| 已接受任务完成 | `test_stop_completes_submitted_tasks` 在 stop 后、析构前检查 1,000 个任务各完成一次；`test_stop_drains_pending_tasks` 控制 A 等待、B/C 排队，确认停止接收后放行并获取结果 |
| 唤醒与 join | 代码检查确认解锁、notify_all、join 顺序；空池关闭及有任务关闭测试正常返回 |
| 反复创建和关闭 | `test_repeated_pool_shutdown` 执行 20 轮，每轮依次 stop 两次再析构，交替覆盖空池与有任务场景 |
| 关闭语义文档 | README 已说明行为、并发约定、生命周期限制及测试范围，并经本人阅读 |

- 验证限制：未运行数据竞争检测器、构造失败注入或全面并发压力测试；没有保证空池测试中每个 worker 在通知前都已阻塞。不支持多个并发 stop、worker 内部 stop 或并发访问正在析构的对象。测试通过不证明所有调度均正确。
- 仍需巩固：独立编写 unique_lock 与条件变量的等待流程、设计可控时序测试、编写带截止时间的探测与失败清理；当前复杂测试由分步教学完成。
- 提交编号或链接：[`3faadee`](https://github.com/Zhihui-Cui/cpp-thread-pool/commit/3faadee19a814433c2f43d662d3a506cb36252e6)，标题为 `feat: add graceful thread pool shutdown`，正文使用 `Refs #6` 关联 Issue；包含上述五个阶段文件，229 行新增、29 行删除。提交时间为北京时间 2026-09-22 10:26:44。本条归档记录由后续文档提交补记。
- GitHub 推送与 Issue 关闭记录：2026-09-22 本人完成推送并反馈成功；本地 `origin/main` 与功能提交编号一致。通过 GitHub 接口核对 Issue #6 的五项验收均已勾选，状态为 `closed`，关闭原因为 `completed`；关闭时间为北京时间 2026-09-22 10:45:12。`Refs #6` 本身不自动关闭 Issue，本次由本人填写验收记录并关闭。归档仅更新文档，沿用上述测试证据，未重复运行测试。

## Issue #7：测试、benchmark 与项目说明

[查看 Issue](https://github.com/Zhihui-Cui/cpp-thread-pool/issues/7)

### 学习范围与文件

- 学习范围：整体测试、性能测量、可复现构建、架构与设计权衡、阶段复盘。
- 本阶段文件：`benchmarks/thread_pool_benchmark.cpp`、`CMakeLists.txt`、`.gitignore`、`include/thread_pool.hpp`、`README.md`、本文件。复用 `tests/` 中的已有功能测试；总控仓库复盘已写入，路径见文末。

### 我的理解与设计选择

以下为依据本阶段对话、当前代码和本人提供的运行输出整理的学习草稿，待本人查阅修改。benchmark 在分步讲解和示例代码指导下完成，不将运行成功或反馈“明白”记为已经能够独立设计实现。

- **先提交，再收集结果**：本人指出提交后立即 `get()` 会在任务尚未执行完成时等待。进一步讲解明确：循环中每提交一个任务就等待，会阻止主线程提交下一个任务，无法让这批任务并行计算。先保存全部 future，再依次 `get()`，只限制结果收集顺序，不限制其他任务的执行。
- **三个职责**：`compute_task(seed, iterations)` 提供确定性的计算负载；`run_benchmark()` 提交一批任务、等待结果并测量耗时；`main()` 计算校验基准、比较不同 worker 配置并输出结果。`BenchmarkResult` 结构体用命名成员同时返回 `elapsed_ms` 和 `checksum`。
- **整数类型与转换**：`std::size_t` 用于任务数量、下标和循环次数；`std::uint64_t` 用于固定 64 位的计算值与总和，无符号结果按模回绕。它不是线程安全类型。`static_cast<std::uint64_t>(i + 1)` 将循环计数转换成任务输入类型，不改变 `i`，也不提供运行时范围检查。`ULL` 表示 `unsigned long long` 整数常量。
- **future 容器**：`reserve()` 预留容量但不添加元素；`push_back()` 保存提交返回的 future。future 不可复制，因此收集结果时使用 `auto&`。worker 各自计算局部变量，主线程独自累加 checksum，没有并发写同一总和。
- **计时边界**：使用 `steady_clock`，计时包含整批提交、调度、计算及 `get()` 汇总；线程池创建、容器预留空间、关闭和输出在计时之外。`duration<double, std::milli>` 将时间间隔表示为浮点毫秒，`count()` 取出数值。
- **校验与错误输出**：`expected_checksum` 是计时之外直接调用同一计算函数得到的串行总和，`checksum` 是线程池结果总和。Release 下用 `if` 校验，避免依赖可能被禁用的 `assert`。`std::cerr` 输出错误信息，本身不会退出程序；`main()` 中的 `return 1` 表示失败退出。总和相等不能严格证明每个任务执行一次，也不能独立证明计算函数正确。
- **重复测量**：每种配置预热一次、正式测量五次，排序后取下标 2 的中位数。每次测量创建新线程池，不复用预热 worker。中位数减少单个异常耗时的影响，但不能消除系统负载和固定测量顺序等偏差。
- **比较口径**：1、2、4 个 worker 使用相同的 1000 个任务、每任务 100000 轮计算和相同初始值。加速比为单 worker 中位数除以当前配置中位数；它不是相对直接串行执行的加速比。更多 worker 不保证总是更快。

2026-09-23 完成一轮架构问答与能力自评。本人能说明空闲 worker 阻塞等待、提交线程通过 `notify_one()` 通知，以及持锁执行任务会阻碍其他线程；也能说明 future 获取结果、结果已就绪时无需等待。任务包装和调用时机仍需提示，具体纠正过程如下；完整线程池的独立重写尚未验证。

### 遇到的问题与解决过程

- **不熟悉 benchmark 的类型与组织**：本人询问结构体、`uint64_t`、`static_cast` 的用途，随后进一步询问 `expected_checksum`、`checksum` 与 `std::cerr`。通过拆解类型、串行与线程池求和示例，以及错误输出和退出码的区别进行讲解；本人反馈理解，独立应用情况仍待后续复述确认。
- **测量方法从单次扩展到多轮**：最初每种配置只运行一次，得到 116.527、60.687、35.564 ms。之后增加一次预热和五次正式测量，保留每轮输出并取中位数；下表使用后者，不将两次运行混为同一组数据。
- **构建产物出现在 Git 状态中**：新增 Release 构建目录后，`build-release/` 被列为未跟踪目录；已在 `.gitignore` 添加 `/build-release/`，后续状态中不再列出该目录。
- 本阶段提供的日志中未出现编译失败或 checksum 不匹配，不补写未经观察的故障。
- **混淆 lambda 创建与调用**：最初认为 `apply` 写在入队之前，所以由提交线程执行。通过捕获初始化与函数体的区别纠正：提交线程保存函数和 tuple；worker 最终调用 `bound_task()` 时才执行 `apply`。本人随后正确回答捕获示例中“saved 已保存，saved + 1 尚未计算”。
- **混淆智能指针与入队任务**：最初描述为将指针直接存入队列，后又把保存动作写成 `ele = (*task)()`。讲解后明确：`shared_ptr` 管理包装任务，`(*task)()` 是立即调用；`[task] { (*task)(); }` 则创建捕获指针的可调用对象，由队列中的 `std::function<void()>` 保存。本人反馈理解，独立重写仍需练习。
- **用职责命名减少混淆**：本人提出变量名 `lambda` 容易与其他 lambda 混淆，已将带参数重载中的局部变量改为 `bound_task`，表达“绑定函数与参数后的任务”；此处仅重命名，不改变调用流程。该改动位于 `include/thread_pool.hpp`，纳入本阶段文件范围。

### 性能测量记录

环境：Windows + PowerShell；Intel Core i5-1240P，12 核心、16 逻辑处理器；MSYS2 UCRT64 GCC 16.2.0（Rev3）；CMake + Ninja，C++17，Release。CPU 与编译器信息来自本人执行 `Get-CimInstance Win32_Processor` 和 `g++.exe --version` 后提供的输出。

| worker 数量 | 第 1 轮（ms） | 第 2 轮 | 第 3 轮 | 第 4 轮 | 第 5 轮 | 中位数（ms） | 相对单 worker 加速比 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 113.710 | 116.841 | 116.156 | 115.861 | 118.277 | 116.156 | 1.00 |
| 2 | 60.052 | 61.200 | 66.277 | 60.658 | 61.059 | 61.059 | 1.90 |
| 4 | 33.690 | 35.004 | 34.469 | 35.002 | 34.943 | 34.943 | 3.32 |

15 次正式测量的 checksum 均为 `11208273736162531860`。预热代码也检查 checksum，但不输出预热耗时。该组数据说明本负载下增加到 2、4 个 worker 缩短了耗时；不能确定 2 worker 第三轮偏慢的具体原因，也不能据此确定最佳线程数或各项同步开销。

构建、运行命令与计时范围见 [README 的 Benchmark 部分](../README.md#benchmark)。上表为本人最初提供的运行证据，保留原始数据。

2026-09-23 最终代码复验：重新构建 Release benchmark 后运行，三种配置的五轮 checksum 均一致，退出码为 0。此次耗时如下，与前一组分开记录，不合并统计：

| worker 数量 | 第 1 轮（ms） | 第 2 轮 | 第 3 轮 | 第 4 轮 | 第 5 轮 | 中位数（ms） |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 150.176 | 158.489 | 155.799 | 139.018 | 136.662 | 150.176 |
| 2 | 98.029 | 67.461 | 68.014 | 66.627 | 67.853 | 67.853 |
| 4 | 38.829 | 52.239 | 38.952 | 82.093 | 67.376 | 52.239 |

复验波动大于之前记录，未采集后台负载、CPU 频率或温度，不能推断具体原因，也不能把两组差异归因于变量重命名。

### 验收与归档

本地要求来自项目已有记录：CMake 构建、CTest 测试、至少两种 worker 数量的 benchmark、完整 README，以及总控仓库复盘。本轮未能连接 GitHub 获取 Issue #7 原文，因此未重新核对远端验收清单和状态。

| 验收内容 | 当前证据与状态 |
| --- | --- |
| CMake 构建 | 本人配置、构建成功；2026-09-23 对最终代码再次完成 Debug 构建和 Release benchmark 构建 |
| CTest | 本人初次记录为 3/3 Passed、0.51 秒；2026-09-23 再次执行 `ctest --test-dir build-ninja --output-on-failure --timeout 30` 为 3/3 Passed、1.17 秒，已核对 Debug 配置 |
| Benchmark | 独立 CMake 目标已添加，Release 下完成 1/2/4 worker 的预热与五轮测量，正式结果均通过 checksum 校验 |
| README | 已补架构、构建运行方式、环境、测量边界、结果和限制，待本人查阅修改 |
| 学习与项目复盘 | 已记录架构问答、纠正过程、能力自评及改进想法；总控仓库阶段复盘已写入，待本人查阅 |

- 实际阶段文件：benchmark 源码、`CMakeLists.txt`、`.gitignore`、`include/thread_pool.hpp` 的局部变量重命名、`README.md`、本文件。2026-09-23 已对重命名后的代码完成构建、测试及 benchmark 复验。工作区原有 `include/single_thread_executor.hpp` 改动不作为本阶段成果自动纳入。
- 功能测试沿用已有用例，本阶段未新增测试用例。三个 CTest 程序包含多个测试函数，不能将 3/3 解释为只有三项断言；现有覆盖与关闭测试见 README。
- 验证限制：未运行数据竞争检测器、构造失败注入或全面并发压力测试。性能仅测一种任务粒度和计算负载，未测直接串行耗时、I/O 负载、更多线程数或空闲 CPU 占用，未单独测量分配、锁及调度开销。
- 待本人查阅：本节及总控仓库复盘是否准确反映自己的理解，复盘中建议的下一阶段练习是否合适。能力、知识缺口及改进想法已按本人 2026-09-23 的回答记录。
- 提交编号或链接：待提交后填写。
- GitHub 推送与 Issue 关闭记录：待完成文档查阅、复盘和验收后填写；当前不标记 Issue #7 已完成或已关闭。

## 跨阶段设计备忘

以下是后续实现时需要检查的设计要点，不代表当前代码已经具备这些功能。

- `condition_variable::wait` 使用谓词处理虚假唤醒。
- 不在持有队列锁时执行用户任务。
- 关闭时唤醒 worker，并确保它们能退出和被 join。
- 单线程的执行顺序测试不能直接证明多 worker 的任务完成顺序。

## 项目完成后的复盘

### 我现在能够独立实现和解释什么

2026-09-23 自评：如果不看代码重写，我认为可以写出线程池类的基本组成，包括队列、线程、锁、结束状态，以及构造、析构和入队的基本框架。这是本人对当前能力的判断，尚未通过完整独立重写验证，尤其不能据此认为所有并发边界已经处理正确。

在问答中能解释 worker 空闲等待与通知、任务应在锁外执行，以及通过 future 获取结果。对捕获初始化和函数体执行时机，经过示例纠正后能够区分；任务包装链路还需要参考。

### 我仍需要补充的知识

不看代码时，目前难以写出条件变量 `cv` 的具体使用、`packaged_task` 与 future 的关联、`submit()` 对函数对象和参数的封装，以及异常抛出与处理代码。

最需要练习的是并发情况下如何管理锁、线程和任务对象：哪些数据需要保护、何时释放锁、线程如何等待和退出、任务及捕获对象应存活多久。后续练习重点是能够推演执行顺序并写出正确代码，而不只是记住接口拼写。

### 如果重新实现，我会调整什么

以下为本人 2026-09-23 提出的后续探索方向，尚未实现或验证，不作为 Issue #7 的新增功能要求。

1. 考虑在分配任务时优先选择等待更久的 worker，探索线程之间的公平性。这是 worker 分配策略，与按任务重要程度选择队列元素的“任务优先级”不同，也不等同于修改操作系统线程优先级。
2. 测试一个 worker 刚完成任务、新任务恰好提交的场景，比较由该 worker 继续执行和交给阻塞中的 worker 两种策略，希望通过系统稳定性与性能表现评估取舍。
3. 目标是安全、高质量且执行快捷的线程池；希望避免冗余设计，以更简洁、轻量的代码完成任务管理。

结合当前代码的说明：现有共享队列未指定哪个 worker 接收任务。新任务入队后，刚完成任务的 worker 和被通知的 worker 都可能取得状态锁并取走任务；`notify_one()` 不是将某个任务指派给某个线程。已经被其他 worker 取空时，醒来的 worker 会重新检查谓词并继续等待。当前实现不保证等待最久的 worker 优先，也不保证均匀分配。

后续实验建议（教学补充，尚未执行）：以当前实现为基线，记录任务是否恰好执行一次、关闭是否完成，再比较整批耗时、任务从提交到开始的等待时间及各 worker 的任务分布。用同步信号控制场景并重复运行，不把一次观察到的分配顺序当成策略保证。严格指定接收 worker 可能需要额外的等待顺序记录或定向交接机制，其同步与维护成本也应纳入评估；线程分配更均匀并不自动代表任务更快完成。

### 总控仓库复盘位置

已于 2026-09-23 写入相邻总控仓库：`ai-infra-roadmap/weekly-reviews/2026-09-23-thread-pool.md`。内容按该仓库的周复盘模板整理，记录本阶段交付、卡点、能力自评和后续方向；建议的下一阶段练习标为待本人确定。文件尚未提交、推送。
